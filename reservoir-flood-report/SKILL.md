---
name: reservoir-flood-report
description: 生成今日水库汛情简报 / 查询当前水库蓄水情况 / 播报全省大中型水库水情。Dynamically scrapes reservoir real-time data from Fujian water resources platform via agent-browser and generates structured flood situation reports in Chinese.
read_when:
  - 生成今日水库汛情简报
  - 查询当前水库蓄水情况
  - 播报全省大中型水库水情
metadata: {"clawdbot":{"emoji":"🌊","requires":{"bins":["node","npm"]}}}
allowed-tools: Bash(agent-browser:*)
---

# 水库汛情简报生成

## 技能描述
使用 Agent 浏览器动态访问福建省水利信息平台的水库实时监测页面，自动提取指定时间段内的大中型水库蓄水统计数据及超汛限水库信息，并严格按照固定模板生成汛情简报。所有数值均来自实时采集的页面数据，杜绝人工填入或虚假生成。

## 触发意图
当用户要求“生成今日水库汛情简报”“查询当前水库蓄水情况”“播报全省大中型水库水情”等，或直接要求使用本技能动态获取数据并输出简报时调用。

## 数据采集流程

### 1. 确定时间范围
- 若用户提供了明确的统计开始时间和结束时间，则使用用户指定的时间（格式 `YYYY-MM-DD HH:MM`）。
- 否则默认统计**当日**，开始时间为 `当日上午 06:00`，结束时间为 `当前系统小时数`（24 小时制，整点），确保简报反映从当日 6 时到最近整点的状况。

### 2. 打开目标页面
- 使用 **Agent 浏览器**（agent-browser 技能）调用edge浏览器无头模式访问以下固定 URL（福建省水利信息平台水库实时水情页面）：
  ```
  http://27.156.118.74:18800/web/html/index.html?module=sqxx
  ```
- 打开页面后，等待关键元素加载完成（等待时间默认不超过 15 秒）。
- **日期筛选设置**：若页面具有日期筛选控件，根据步骤 1 确定的时间范围设置起止时间并点击查询按钮，等待数据刷新；若无显式控件或数据已默认为当日实时数据，则直接采集当前页面内容。

### 3. 数据提取与映射
利用 Agent 浏览器的内容解析能力，从页面文本、表格或结构化元素中精确提取字段，如没有直接的字段数据请总结分析相关数据，填写字段值。可根据页面实际 DOM 结构灵活使用选择器、正则表达式或表格解析，但必须保证提取逻辑可复现。

| 技能参数 | 页面来源与提取说明 |
|----------|-------------------|
| `large_reservoirs` | 页面中“大型水库”后的数字（座数），如“大型水库 23 座”中提取整数 23 |
| `medium_reservoirs` | 页面中“中型水库”后的数字（座数） |
| `total_storage` | 页面中“蓄水总量”或“总蓄水量”对应的数值，单位亿立方米，提取为浮点数（如 `85.636`） |
| `normal_capacity_ratio` | 页面中“占正常库容”或“正常库容比例”后的百分数，提取数值部分（如 `51.52`） |
| `highlight_reservoir` | 页面中“超汛限水位水库列表”或含“水库名称”“水位”“超汛限”等列名的表格。提取每座水库的 `name`、`water_level`（米）、`exceed_limit`（米）。若页面仅给出当前水位与汛限水位，则按 `exceed_limit = 当前水位 - 汛限水位` 计算；若未单独列出超汛限值，则根据水位差判断是否超限，并填入差值 |

**提取优先级**：
1. 优先识别表格（`<table>`）中对应列名，自动匹配提取。
2. 若为卡片布局，则通过关键词邻近的数字文本进行提取。
3. 若数据以图表展示，尝试切换为表格视图或检查图表附带的数据接口；无法切换时，向用户报告数据不可用并终止简报生成。

### 4. 数据校验
- `large_reservoirs`、`medium_reservoirs` 均为正整数，两者之和一般处于 10~500 之间。
- `total_storage` > 0，`normal_capacity_ratio` 介于 0 到 100 之间。
- `highlight_reservoir` 可为空列表（表示无超汛限水库），每条记录须包含水库名、水位、超汛限值（可为 0 或负值表示未超限，此时简报不输出该水库）。
- 若关键数据缺失（返回 null、0 或 NaN），注明“数据获取不完整”并停止输出，提示用户稍后重试。

## 输出模板
严格按照以下格式输出，其中 `{}` 内的占位符替换为实际采集数据。

```
{start_day}日{start_hour}时~{end_hour}时，全省共有{large_reservoirs}个大型水库，{medium_reservoirs}个中型水库，
大中型水库蓄水总量{total_storage}亿立方米，占正常库容{normal_capacity_ratio}%。其中:
{highlight_list}
```

### 占位符计算规则
- `{start_day}`：`start_time` 中的日期数字（不加前导零，如 `6`）。
- `{start_hour}`：`start_time` 中的小时数（24 小时制，不加前导零）。
- `{end_hour}`：`end_time` 中的小时数（24 小时制，不加前导零）。
- `{large_reservoirs}`、`{medium_reservoirs}`：输出提取的整数。
- `{total_storage}`：保留三位小数（如 `85.636`）。
- `{normal_capacity_ratio}`：保留两位小数（如 `51.52`）。
- `{highlight_list}`：每个超汛限水库一行，格式 `{name}水位{water_level}米,超汛限{exceed_limit}米。` 多个条目之间换行，`其中:` 后直接换行，不添加序号或额外标点。

## 示例输出
```
28日6时~18时，全省共有23个大型水库，196个中型水库，大中型水库蓄水总量85.636亿立方米，占正常库容51.52%。其中:
延平区沙溪口水库水位85.69米,超汛限0.72米。
建阳区雷公口水库水位152.11米,超汛限1.03米。
```

## 异常处理与质量保障
1. **页面无法访问**：尝试重试一次（间隔 3 秒）；若仍失败，回复“当前水情页面暂时无法访问，请稍后重试”。
2. **数据不完整**：若提取到的水库座数或蓄水量为 0 或 NaN，提示“页面关键数据缺失，无法生成简报”。
3. **时间格式异常**：若用户指定的时间解析失败，默认使用当日 6 时~当前小时。
4. **超汛限水库列表为空**：`highlight_list` 部分输出“暂无超汛限水位水库。”，但保留 `其中:` 引导语。
5. **数据一致性**：如果大中型水库座数总和明显不合理（例如 >500 或 <10），在简报末尾附加一句“(注：水库数量已自动校验，与历史均值差异较大，请人工核实)”。

## 注意事项
- 采集和简报中所有数值直接使用提取结果，禁止四舍五入或改变精度。
- 日期和小时不补零，保持与模板示例一致。
- 输出结尾不加多余标点、空行。
- 如果用户提供了数据（如 JSON）作为补充，优先使用动态采集结果，用户数据仅用于交叉验证。
- 本技能强依赖 Agent 浏览器（agent-browser）的真实浏览能力进行数据采集，严禁模拟、缓存或编造数据。所有采集步骤必须通过 agent-browser 执行，页面 URL 固定为 `http://27.156.118.74:18800/web/html/index.html?module=sqxx`，不得替换为其他来源。
